//Janky help command

module.exports = {
    name: 'help',
    description: "lists all avaliable commands",
    execute(message,args){
        //Collector.Add('This is a full map of the planned commands.' + {files: ['../Resources/Images/commandMap.png']});
        
        //file can not be attatched at this time due to the collector not supporting it. 
    }
}